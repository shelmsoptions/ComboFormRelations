<h1>Jobs Index</h1>
<table>
  <thead>
    <th>Job Code</th>
    <th>Company</th>
  </thead>
  <tbody>
    <% @jobs.each do |job| %>
    <tr>
      <td><%= job.job_code %></td>
      <td><%= job.company.name %></td>
    </tr>
    <% end %>
  </tbody>
</table>

<%= link_to 'New Job', new_job_path %>